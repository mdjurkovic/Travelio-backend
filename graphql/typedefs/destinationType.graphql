# Types
type DestinationType implements Timestamps {
    id: ID!
    label: String!
    destinations: [Destination]

    # Interface required
    createdAt: Date!
    updatedAt: Date!
}

# Inputs
input DestinationTypeCreateInput {
    id: ID!
    label: String!
}

input DestinationTypeUpdateInput {
    label: String
}
